<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script type ="text/javascript" src="/socket.io/socket.io.js"></script>
    <script type ="text/javascript">
         $(document).ready(function (){

            if(name == undefined || name == null){
                // var x;
                var name=prompt("Your name:");
            }
            console.log(name)
            // if (name!=null){
            //     // x="Hello " + name + "! How are you today?";
            //     // alert(x);
            // }


            $('#send').click(function (e){
                var chat = document.getElementById("box").innerHTML
                var message = $('#message').val()
                e.preventDefault()
                $('#box').append("<p>"+name+" : "+message+"</p>")
                $('#message').val() = ""

            });

            var socket  = io.connect();
            $('#button').click(function (){
                socket.emit( "button_clicked", {clicked:true});
            });
            socket.on( 'server_response', function (data){
                // console.log( 'The server says: '  + data.response);
                $('span').text(data.response)
            });        



    })
    </script>


</head>
<body>
    <div>
        <h2>Conversation Board</h2>
        <div id="box" style="border: 1px solid black; width: 600px; height: auto; padding:10px ">
        
        
        </div>
        <br>
        <form>
            <input type="text" name="" id="message" placeholder="enter your message here" style="border: 1px black solid; width: 300px;">
            <input type="submit" name="send" id="send" value="Send">
        </form>
       
    </div>
</body>
</html>